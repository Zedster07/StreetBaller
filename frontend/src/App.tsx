import { Router } from '@/config/router';

/**
 * App Component - Root Component for StreetBaller Frontend
 * 
 * Routes all pages through React Router
 */

export default function App() {
  return <Router />;
}
